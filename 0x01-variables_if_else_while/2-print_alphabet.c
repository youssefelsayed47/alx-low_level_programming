#include <stdio.h>

/**
 * main - Entry point
 * Description: print all aplhabet letters
 * Return: Always 0 (Success)
*/
int main(void)
{
	for (int i = 97; i <= 122; i++)
	{
		putchar(i);
		ch++;
	}
	putchar('\n');
	return (0);
}
